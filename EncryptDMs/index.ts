import { Plugin } from "aliucord/entities";
import { ApplicationCommandOptionType } from "aliucord/api";

export default class EncryptDMs extends Plugin {
    public async start() {
        // TODO figure out how the heck to get crypto in rn
    }
}